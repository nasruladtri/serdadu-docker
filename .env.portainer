# Environment Variables untuk Portainer Deployment
# ===================================================
# Copy variabel-variabel ini ke Portainer Stack Environment Variables
# atau gunakan sebagai referensi untuk konfigurasi deployment

# ===================
# APPLICATION SETTINGS
# ===================
# Nama aplikasi
APP_NAME=SERDADU

# Environment: production, staging, local
APP_ENV=production

# Debug mode: HARUS false di production!
APP_DEBUG=false

# Application Key: Generate dengan: docker compose exec app php artisan key:generate
# PENTING: Harus diisi sebelum deployment!
APP_KEY=

# URL aplikasi (sesuaikan dengan domain Anda)
# Contoh: https://serdadu.yourdomain.com atau http://your-server-ip:8000
APP_URL=http://localhost:8000

# Timezone aplikasi
APP_TIMEZONE=Asia/Jakarta

# Locale aplikasi
APP_LOCALE=id

# ===================
# DATABASE CONFIGURATION
# ===================
# Nama database
DB_DATABASE=serdadu

# Username database (bukan root!)
DB_USERNAME=serdadu_user

# Password database user
# PENTING: Ganti dengan password yang kuat!
DB_PASSWORD=ChangeMe123!

# Password root database
# PENTING: Ganti dengan password yang kuat!
DB_ROOT_PASSWORD=RootChangeMe123!

# Port database (default: 3306)
# Ubah jika port 3306 sudah digunakan
DB_PORT=3306

# ===================
# APPLICATION PORTS
# ===================
# Port untuk mengakses aplikasi web
# Contoh: 8000, 80, 8080
APP_PORT=8000

# Port untuk phpMyAdmin (hanya jika menggunakan profile dev)
PMA_PORT=8080

# Port untuk Redis (hanya jika menggunakan profile cache)
REDIS_PORT=6379

# ===================
# SESSION & CACHE
# ===================
# Session driver (database recommended untuk production)
SESSION_DRIVER=database

# Session lifetime dalam menit
SESSION_LIFETIME=120

# Encrypt session (HARUS true di production!)
SESSION_ENCRYPT=true

# Cache store
CACHE_STORE=database

# ===================
# LOGGING
# ===================
# Log channel
LOG_CHANNEL=stack

# Log level: debug, info, notice, warning, error, critical, alert, emergency
LOG_LEVEL=error

# ===================
# REDIS (OPTIONAL)
# ===================
# Uncomment jika menggunakan Redis
# REDIS_CLIENT=phpredis
# REDIS_HOST=redis
# REDIS_PASSWORD=null
# REDIS_PORT=6379

# ===================
# MAIL CONFIGURATION (OPTIONAL)
# ===================
# Mail driver
MAIL_MAILER=log

# SMTP settings (jika menggunakan SMTP)
# MAIL_HOST=smtp.mailtrap.io
# MAIL_PORT=2525
# MAIL_USERNAME=null
# MAIL_PASSWORD=null
# MAIL_FROM_ADDRESS=noreply@serdadu.local
# MAIL_FROM_NAME="${APP_NAME}"

# ===================
# CATATAN PENTING
# ===================
# 1. Pastikan APP_KEY sudah di-generate sebelum deployment
# 2. Ganti semua password default dengan password yang kuat
# 3. Sesuaikan APP_URL dengan domain/IP server Anda
# 4. Untuk production, pastikan APP_DEBUG=false dan SESSION_ENCRYPT=true
# 5. Backup database secara berkala
